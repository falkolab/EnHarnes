# Architecture policy — layer structure for this project.
# Defines layers, allowed imports between them, and code conventions.
#
# Used by:
#   .claude/skills/harness.linters/scripts/architecture-health/test_layer_dependencies.py
#   .claude/skills/harness.linters/scripts/code-health/code_conventions.py
#
# See example/ for a working reference with 7-layer architecture.
# TODO: [AI] Hook to sync from ARCHITECTURE.md automatically.


layers:
#  - Types
#  - Config
#  - Repo
#  - Service
#  - Runtime
#  - UI
#  - Providers

allowed_imports:
#  Types: []
#  Config: [Types]
#  Repo: [Config, Types]
#  Service: [Repo, Config, Types, Providers]
#  Runtime: [Service, Repo, Config, Types, Providers]
#  UI: [Runtime, Service, Config, Types, Providers]  # Rule 3: no Repo
#  Providers: [Service, Repo, Config, Types]

# The single layer permitted to import cross_cutting_modules. Read by
# test_layer_dependencies.py; defaults to Providers when absent, so renaming
# the layer here is enough — no code change.
cross_cutting_layer: Providers

cross_cutting_modules:
#  - auth
#  - logging
#  - tracing
#  - secrets
#  - config_loader

file_size:
  soft_limit: 500
  hard_limit: 1500
